-- Глибина брифів ("повзунок глибини" в адмінці, продовження 005_kb_briefs.sql
-- brief_model/brief_language). Одна настройка обслуговує ОБИДВА генератори
-- брифів: briefing.make_brief (бриф під ліда) і mcp/chat.py chat_brief
-- (синтез усього AI-чату в звіт) — обидва читають один і той самий
-- brief_max_words замість двох незалежних налаштувань.
--
--   brief_max_words   ціль довжини брифа в словах. Python-шар клемпить це
--                     100..2000 і мовчки падає на дефолт при кривому значенні
--                     (порожній рядок, текст замість числа) — заслати
--                     некритичну настройку в 500 нема причин. Дефолт нижче —
--                     той самий, що й хардкод "350 words", який ця настройка
--                     заміняє в briefing._SYSTEM.
--
-- public. — свідомо: n8n має ВЛАСНУ таблицю settings (той самий трюк
-- search_path, що зламав пороги в 626c636), тож пишемо повним іменем, як і
-- 009_chat_agent2.sql.

INSERT INTO public.settings (key, value) VALUES
    ('brief_max_words', '350')
ON CONFLICT (key) DO NOTHING;
