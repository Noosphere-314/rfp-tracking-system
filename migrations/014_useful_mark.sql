-- «Корисна чи ні» для БУДЬ-ЯКОЇ знахідки (запит Миколи 2026-08-11: у
-- стовпчику Outcome нема кнопок — бо Won/Lost навмисно лише для лідів,
-- доля УГОДИ; а людина хоче оцінювати самі знахідки).
--
-- Окреме поле, а НЕ нові значення в outcome: won/lost — це доля угоди по
-- ліду (win-rate для калібрування порога), useful — якість самої знахідки
-- (сигнал для порогів/ключових слів). Змішування зіпсувало б обидві
-- метрики. NULL = ще не оцінено.

ALTER TABLE seen_items
    ADD COLUMN IF NOT EXISTS useful boolean;
